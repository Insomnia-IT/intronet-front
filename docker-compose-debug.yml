version: "3.6"
services:
  web:
    image: nginx:latest
    container_name: intronet-front-debug
    volumes:
      - ./build:/app
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 5001:80

